package whiteboard.test;

import org.junit.Test;

/**
 * Test suite for methods in User.
 * 
 * We unit test the constructors, input, output, equals, toJSON, fromJSON
 * methods. We will manually test the draw method.
 * 
 * @author Merry
 * 
 */
public class UserTest {

    @Test
    public void equalsTest() {
        // test the equals and constructor methods
        // TODO // assertTrue(d.equals(d1));
    }

    @Test
    public void toJSONFromJSONEqualsTest() {
        // test the toJSON, fromJSON methods
        // TODO // assertTrue(d.equals(DrawableSegment.STATIC.fromJSON(d.toJSON().toJSONString())));
    }
}
